server:
  port: 8080
spring:
  application:
    name: VRQuestion
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/vrquestion?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 314914.fgz
  # session 失效时间（分钟）
  session:
    timeout: 1000
    store-type: redis
  # redis 配置
  redis:
    port: 6379
    host: 192.168.208.130
    database: 2
    password: 314914.fgz
mybatis-plus:
  configuration:
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
    map-underscore-to-camel-case: false
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      update-strategy: not_null
      id-type: auto
      logic-delete-field: isDelete
      logic-delete-value: 1
      logic-not-delete-value: 0
logging:
  level:
    com.hmall: debug
  pattern:
    dateformat: HH:mm:ss:SSS
  file:
    path: "logs/${spring.application.name}"
knife4j:
  enable: true
  openapi:
    title: 声趣配音项目
    description: "声趣配音项目"
    email: 153754997@qq.com
    concat: feng
    url: https://www.soundfun.cn
    version: v1.0.0
    group:
      default:
        group-name: default
        api-rule: package
        api-rule-resources:
          - com.feng.soundfundub.controller

